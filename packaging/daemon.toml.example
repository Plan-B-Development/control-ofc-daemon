# Control-OFC Daemon Configuration
# Copy to /etc/control-ofc/daemon.toml and edit as needed.
# All fields are optional — defaults are shown below.

[serial]
# Use stable /dev/serial/by-id/ path (survives USB re-enumeration)
# port = "/dev/serial/by-id/usb-Karanovic_Research_OpenFan_...-if00"
# timeout_ms = 500

[polling]
# poll_interval_ms = 1000

[ipc]
# socket_path = "/run/control-ofc/control-ofc.sock"

[state]
# state_dir = "/var/lib/control-ofc"

[startup]
# Delay in seconds before the daemon begins device detection after boot.
# Useful for waiting for USB or hwmon devices to appear.
# delay_secs = 0

[profiles]
# Directories where the daemon looks for profile JSON files.
# The GUI stores profiles at ~/.config/control-ofc/profiles/ by default.
# Add your user's profile directory for GUI profile activation to work.
# search_dirs = ["/etc/control-ofc/profiles", "/home/yourusername/.config/control-ofc/profiles"]
